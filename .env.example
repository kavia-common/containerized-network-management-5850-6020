# Frontend environment variables
# Base path for backend API. In this environment the backend is exposed at the same origin via /api.
REACT_APP_API_BASE=/api

# Backend preview port (for documentation only)
# FRONTEND_PREVIEW_PORT=3000

